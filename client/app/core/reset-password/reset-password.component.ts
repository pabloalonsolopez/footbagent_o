import { Component } from '@angular/core'

@Component({
  moduleId: module.id,
  selector: 'fb-reset-password',
  templateUrl: 'reset-password.component.html',
  host: {
  	class: 'w(100%) maxw(40rem) m(auto) p(2rem)'
  }
})

export class ResetPasswordComponent { }